name: Development CI

on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development

jobs:
  lint-and-test:
    name: Lint and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          echo "Disk space before cleanup:"
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf "/usr/local/share/boost"
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
          sudo docker system prune -af
          echo "Disk space after cleanup:"
          df -h

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Python dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install flake8 black pytest pytest-cov

      - name: Install Node.js dependencies
        run: |
          cd frontend
          npm ci

      - name: Lint Python code (flake8)
        continue-on-error: true
        run: |
          cd backend
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Format check Python code (black)
        continue-on-error: true
        run: |
          cd backend
          black --check --diff .

      - name: Lint TypeScript/JavaScript (eslint)
        continue-on-error: true
        run: |
          cd frontend
          npm run lint || echo "ESLint not configured, skipping..."

      - name: Run Python unit tests
        continue-on-error: true
        run: |
          cd backend
          if [ -d "tests" ]; then
            pytest tests/ -v --cov=app --cov-report=term-missing || echo "Tests failed or not found"
          else
            echo "No tests directory found, skipping tests"
          fi

      - name: Run TypeScript tests
        continue-on-error: true
        run: |
          cd frontend
          npm test -- --passWithNoTests || echo "Tests not configured, skipping..."

      - name: Summary
        if: always()
        run: |
          echo "## Development CI Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Code linting completed (warnings allowed)" >> $GITHUB_STEP_SUMMARY
          echo "✅ Tests executed" >> $GITHUB_STEP_SUMMARY
          echo "✅ Ready for PR to staging branch" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Note:** Docker builds are validated in staging deployment, not here." >> $GITHUB_STEP_SUMMARY
