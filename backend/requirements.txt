fastapi==0.110.1
pdfplumber==0.10.2
PyMuPDF==1.24.2
pandas==2.2.2
openpyxl==3.1.2
uvicorn
python-multipart
PyJWT==2.8.0  # CRITICAL: Pinned to 2.8.0 for Docker compatibility (see DOCKER_JWT_COMPATIBILITY.md and docker-entrypoint.sh)
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
email-validator==2.1.0
sqlalchemy==2.0.*

# Celery and Redis for background processing
celery==5.3.*
redis==5.0.*

# Smart Bank Statement parsing dependencies
pdf2image==1.17.*
python-doctr[torch]==0.8.*
xlsxwriter==3.2.*
pillow>=9.0.0

# Intelligent column detection dependencies
scikit-learn==1.4.*
numpy>=1.26.0  # Updated for Python 3.12 compatibility in Docker

# AWS Textract dependencies
boto3==1.34.*
python-dotenv==1.0.*

# New API features dependencies
httpx==0.27.*  # For async HTTP requests in webhooks
aiohttp==3.9.*  # For async HTTP client requests
croniter==2.0.*  # For cron-style scheduling
pydantic[email]==2.7.*  # For enhanced validation

# Azure Document Intelligence dependencies
azure-ai-documentintelligence==1.0.*  # For Azure Document Intelligence SDK

# Azure AI Translator dependencies  
azure-ai-translation-text==1.0.*  # For Azure AI Translator Text API
azure-storage-blob==12.19.*  # For Azure Blob Storage integration 